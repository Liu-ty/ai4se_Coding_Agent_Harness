# Coding Agent Harness 项目反思

## 一、从“万能 Agent”到可靠闭环

项目开始时，我想构建一个能覆盖多种编程场景的通用 Coding Agent：它能读写文件、运行命令、调用 API 并自我纠错。这个设想很吸引人，却没有回答系统凭什么可靠。Brainstorming 阶段的问题可以概括为：哪些保证应由确定性代码提供，哪些行为仍依赖 LLM 的不确定决策？这让我意识到，LLM 只是产生下一步动作，真正决定系统质量的是 harness 如何组织上下文、约束工具、拦截危险并反馈客观结果。

因此，我放弃功能广度，把“可复现失败—受控修改—自动验证—结构化反馈—再次决策”确定为主要贡献。通用性降低虽有遗憾，但核心机制能在移除真实 LLM 后由 mock 验证，也让我更踏实。如果重做，我会更早锁定这一边界。

## 二、Superpowers：约束价值与形式化成本

对我帮助最大的是 `brainstorming` 和 `writing-plans`。前者迫使我在实现前回答“为什么做、重点做深什么、什么明确不做”；后者把这些判断转换为包含文件、接口、失败测试和验收命令的任务合同。出现偏差时，我可以依据书面约定评审，而不是只凭感觉返工。

流程也有成本。PR #4 在三条 CodeRabbit 线程尚未解决时提前合并，随后必须由 PR #6 补齐评审闭环。这说明省略流程的代价会延后出现，但严格流程也不能替代判断。安全、状态机、并发和跨平台逻辑值得保留完整证据；纯文档和低风险修改则可采用轻量流程。

## 三、TDD 是放大器

在本项目中，TDD 更像放大器：它既放大清晰规约的收益，也放大模糊规约的返工成本。状态转换、路径逃逸、凭据泄露、审批生命周期和反馈分类都适合先用失败测试固定边界。

Task 17 的审批生命周期修复给我留下了最深印象。原实现进入 `AWAITING_APPROVAL` 后关闭了创建 run 的 runtime，后续进程无法在原 controller 上恢复动作。新的真实组合测试连接 SQLite、agent loop 和本地 HTTP 审批路由，RED 阶段首先因缺少 `continueRun` 失败，GREEN 阶段则要求同一个 runtime 完成审批并进入终态。没有这种测试，局部组件都可能“能跑”，真实审批流程却无法继续。

代价同样存在。早期配置、存储和预算模块曾因 `Load` 返回契约、store port 归属和 budget/no-progress 语义发生对齐，测试也随之调整；Windows 进程与前端竞态的验证成本尤其高。因此，TDD 不是自动提高质量的仪式，它的效果高度依赖 SPEC 与 PLAN 是否清晰。

## 四、Subagent 自主性与任务颗粒度

Subagent 能自主推进多远，主要取决于任务是否拥有闭合上下文，而不是 prompt 有多长。接口局部、命令确定的任务可以交给新鲜 subagent 独立完成；一旦问题跨越 runtime、持久化、HTTP 安全和前端状态，局部测试通过也不能证明整体生命周期正确。

我认为合适的颗粒度是“一个稳定接口或一种失败模式，加上一组独立验收测试”。任务过大会让 agent 在长上下文中偏离，过小又会让 review 和合并成本压过实现。冷启动 agent 将 Windows 路径测试移到 Windows-only 文件，让当前测试变绿，却绕开了“所有宿主平台都应识别 Windows 绝对路径”的要求。

## 五、SPEC、PLAN 与冷启动验证

冷启动验证最能体现书面规约的价值。OpenCode/DeepSeek V4 Pro 只读取 `SPEC.md` 与 `PLAN.md` 后实现 Tasks 1–2，并自评 PASS；独立 verifier 却发现，PLAN 已固定 `Load(io.Reader) (Config, error)`，实现仍返回了 `*Config`。其他问题还包括依赖宿主 OS 的路径判断、遗漏 timeout 示例、未明确 `default_profile` 验证，以及 `CommandSpec` 缺少 classifiers。

这些缺陷来自主开发上下文中的“当然如此”。我随后修订 PLAN、合同测试和实现，补充跨平台用例与编译期接口约束；SPEC 的产品范围未改变。此后，我把“只提供当前 task 所需的接口、文件、失败测试、验收命令和非目标”作为主要的 prompt/context engineering 策略，并让新鲜 reviewer 分别检查 spec 合规与代码质量，避免继承未经验证的假设。

## 六、凭据与分发：从能跑到可交付

凭据要求让我认识到，安全录入不等于把 key 放进环境变量。项目还必须考虑终端 history、日志、子进程继承、SQLite/WAL 残留、兼容 endpoint 误绑定，以及公开 demo 是否注册真实能力。最终实现优先使用 OS keyring，并提供加密 vault fallback；真实凭据只属于本地 CLI 路径，公开容器则是刻意裁剪凭据与真实执行能力的 mock demo。

分发同样不止一个 `Dockerfile`。Task 18 建立了用于生成 Release、checksum 和 GHCR 镜像的 workflow，以及 Compose/Caddy 部署资产；但正式 tag、可下载产物、目标机验证和访问链接仍须在提交前实际完成。这项尚待闭环的工作本身就是教训：凭据威胁模型、CI 骨架和 release smoke test 应更早建立。

## 七、对 Superpowers 的批判与重做设想

Superpowers 隐含了若干并非总成立的假设：需求能预先形式化，任务能按接口切分，客观测试能覆盖真实风险，新鲜 subagent 能凭局部上下文正确实现，review gate 能在合并前阻断问题。这些假设对配置解析、路径策略和确定性反馈较为成立，对跨组件生命周期、异步竞态和平台进程行为只部分成立。局部成功不能自动推出端到端正确。

它还容易把“保留过程证据”异化为“生产过程文本”。日志过细会降低可读性，过期状态还可能与 Git 历史冲突。若重做，我会更早建立真实 composition 生命周期测试、Windows/Linux 边界测试，以及从 tag 到下载运行的 release smoke test；同时让 `AGENT_LOG.md` 只保留决策摘要与证据链接，把逐轮输出放入独立 task report。

我还会调整“每个 task 完成后更新 PLAN 状态”的执行方式：PLAN 只记录 commit hash 和一句结论，详细理由进入 PR 或 AGENT_LOG。这样既保留追踪性，也避免状态更新产生大量掩盖真正变更的 diff。

## 八、结语

这个项目让我对课程命题有了更具体的回答：当 LLM 能完成大量编码工作时，工程师的价值在于决定做什么、不做什么，建立可验证的边界，并在系统失效时定位责任。Superpowers 能守住 TDD、计划和评审纪律，却不能替我决定产品范围，也不能替我判断一项通过测试的实现是否真正满足规约。正是在这些取舍、质询和纠偏中，我承担了项目的最终责任。

> **AI 辅助披露：** 本文写作过程中使用 AI 辅助梳理结构、核查仓库事实并进行语言压缩；所有经历、判断和批判性观点均由本人核对并负责。
